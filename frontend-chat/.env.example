# Candidate chat (Next.js) — copy to `.env.local` (recommended) or `.env` for `npm run dev`.
# Docker Compose does not use this file: it passes NEXT_PUBLIC_API_URL from the repo root `.env`
# as a build arg (see docker-compose.yml).

NEXT_PUBLIC_API_URL=http://localhost:8000
